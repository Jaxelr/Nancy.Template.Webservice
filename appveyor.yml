version: '1.0.{build}'
image: Visual Studio 2017
branches:
  only:
  - master
init:
  - cmd: git config --global core.autocrlf true
before_build:
  - cmd: dotnet --version
build_script:
  - cmd: dotnet publish ./src/Nancy.Template.WebService.csproj -v quiet
test_script:
  - cmd: dotnet test ./tests/Nancy.Template.WebService.Tests.csproj  -v quiet 
skip_commits:
  files:
    - '**/*.md'
